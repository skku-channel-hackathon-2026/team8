/**
 * 서버를 쓸지, 이 기기 안에서만 돌지.
 *
 * `false`면 앱은 서버도 DB도 부르지 않는다. 화면과 기능은 그대로고, 상대가
 * 하는 일(신청 수락·부탁 맡기·채팅 답장)은 tick()이 흉내 낸다. 네트워크가
 * 없어도, 채널톡 밖에서도, 심사위원 앞에서 혼자서도 끝까지 돌아간다.
 *
 * `true`면 같은 동작이 서버로 나가고 여러 사람이 같은 데이터를 본다.
 * 서버 코드(server/, /api/*)는 둘 중 어느 쪽이든 그대로 남아 있으므로,
 * 이 한 줄만 바꾸면 오간다.
 *
 * 지금 false인 이유: 배포 환경에서 인증이 풀리지 않아 시연을 막고 있었다.
 * 원인을 잡으면 true로 되돌린다.
 */
export const USE_SERVER = false
